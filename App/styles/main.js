import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    body: {
        display: 'flex',
        alignContent: 'center',
        alignItems: 'center'
    },
    
})

export default styles;
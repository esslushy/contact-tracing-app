import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    body: {
        display: 'flex',
        flex: 1,
        alignItems: 'center',
        margin: 30,
    },
    formItems: {
        width: '100%',
        padding: 10,
    },
    labelText: {

    },
    submitButton: {
        height: 100
    }
})

export default styles;